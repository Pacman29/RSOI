using System;
using System.Linq;
using Xunit;

namespace UnitTestProject1
{
    /*public class UnitTest1 : IClassFixture<WebApplicationFactory<Startup>>
    {
        private readonly WebApplicationFactory<RSOI.Startup> _factory;

        public JobControllerTest(WebApplicationFactory<Startup> applicationFactory)
        {
            _factory = applicationFactory;
        }

        [Theory]
        [InlineData("/api/job")]
        public async Task Index200(string url)
        {
            var client = _factory.CreateClient();
            var response = await client.GetAsync(url);
            Console.Write("test");
        }
    }*/
}
